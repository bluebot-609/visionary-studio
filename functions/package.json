{
  "name": "visionary-studio-functions",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "lint": "eslint src --max-warnings=0",
    "serve": "npm run build -- --watch",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@google/genai": "^1.26.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.6.0",
    "razorpay": "^2.9.2"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
